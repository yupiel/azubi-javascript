var assert = require('assert');

describe('Object', function () {
    describe('keys()', function () {
        it('should return ??? of an empty object', function () {

            let givenObject = {};
            let actual = givenObject.keys();
            let expected = '???';

            assert.equal(actual, expected);
        });
    });
});