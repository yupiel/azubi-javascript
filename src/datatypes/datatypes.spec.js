var assert = require('assert');

describe('integer', function () {
    describe('addition', function () {
        it('addition of two numbers should be the sum', function () {

            let firstNumber = 1;
            let secondNumber = 2;
            let actual = firstNumber + secondNumber;
            let expected = '???';

            assert.equal(actual, expected);
        });
    });
});